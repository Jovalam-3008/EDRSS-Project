# Notebooks EDRSS V2

Los notebooks se leen y ejecutan en orden numérico. Cada uno contiene: objetivo, entradas, proceso, validaciones, resultado y siguiente paso.

Los notebooks no contienen contraseñas ni bloques extensos de SQL. Las transformaciones viven en la carpeta `sql/` y las funciones reutilizables en `src/`.

